sudo: required
services:
  - docker
  
before_install:
  -docker build -t praveenbapuram/docker-react  -f Dockerfile.dev .
  
script:
  - docker run praveenbapuram/docker-react npm run test -- --coverage